export const environment = {
  enableProdMode: true
};

(<any>window).token = 'token_dev';

(<any>window).urls = {
  operation: 'http://operation.dev.cloudtogo.cn',
  sso: 'http://sso.dev.cloudtogo.cn'
};
